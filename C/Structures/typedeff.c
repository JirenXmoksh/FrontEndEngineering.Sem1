#include<stdio.h>
typedef float realNumber; // We shifter the properties of 'float' data type to our self made data type 'realNumber'
//typedef oldName newName;
//Therefore, It can be used to rename the functions
int main(){
    realNumber y = 1.232424;
    printf("%f", y);
    return 0;
}